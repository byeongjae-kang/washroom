import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    width: "100%"
  },
  background: {
    height: 280,
    resizeMode: "contain"
  },
  authwrapper: {
    alignItems: "center"
  }
});

export default styles;
