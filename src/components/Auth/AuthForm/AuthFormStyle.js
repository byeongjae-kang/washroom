import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    width: 350,
    height: 45,
    paddingLeft: 5,
    fontSize: 30,
    fontWeight: "bold"
  },
  buttonText: {
    color: "white",
    fontSize: 20,
    fontWeight: "bold",
    textAlign: "center"
  }
});

export default styles;
