import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems:'center'
  },
  title: {
    height: 50,
    padding: 5,
    fontSize: 30,
    fontWeight: "bold"
  }
});


export default styles;
